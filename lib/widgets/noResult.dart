import 'package:flutter/cupertino.dart';

import 'myTextFieldForm.dart';

Widget NoResult(){
  return Center(
      child: Container(
        width: 200,
        child: Image.asset("assets/no_data.png"),
      )
  );
}